/**
 * File Logger — re-export from @elio/shared (canonical source)
 */

export {
  fileLogger,
  createFileLogger,
  setMinLevel,
  LOG_PATHS,
  type FileLogLevel as LogLevel,
  type LogEntry,
} from '@elio/shared';
